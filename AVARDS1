<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<title>Pet Vote</title>
<script src="https://telegram.org/js/telegram-web-app.js"></script>
<style>
    body {
        font-family: Arial, sans-serif;
        text-align: center;
        margin: 0;
        padding: 50px 10px;
        background-color: #FFF8F0;
    }

    /* Новый заголовок в стиле Felix */
    .felix-style-title {
        font-family: 'Bangers', cursive; /* мультяшный шрифт */
        font-size: 72px;
        font-weight: bold;
        text-align: center;
        background: linear-gradient(90deg, #FF6A00, #FFCC00, #FF6A00);
        -webkit-background-clip: text;
        color: transparent;
        text-shadow:
            4px 4px 0 #000,
            8px 8px 0 rgba(0,0,0,0.3),
            0 0 12px rgba(255,200,0,0.7);
        animation: glow 2s infinite alternate ease-in-out;
        margin-bottom: 50px;
        position: relative;
        display: inline-block;
    }

    /* Ушки сверху букв */
    .felix-style-title::before {
        content: "🐱🐱"; /* можно заменить на изображение, если хочешь */
        position: absolute;
        top: -50px;
        left: 10%;
        font-size: 40px;
    }

    /* Хвостики снизу */
    .felix-style-title::after {
        content: "🐾🐾"; /* можно заменить на маленькие картинки */
        position: absolute;
        bottom: -40px;
        right: 10%;
        font-size: 30px;
    }

    @keyframes glow {
        0% {
            text-shadow:
                4px 4px 0 #000,
                8px 8px 0 rgba(0,0,0,0.3),
                0 0 12px rgba(255,200,0,0.8);
        }
        100% {
            text-shadow:
                4px 4px 0 #000,
                8px 8px 0 rgba(0,0,0,0.3),
                0 0 25px rgba(255,200,0,1);
        }
    }

    /* Кнопки лапок */
    .paw-container {
        display: flex;
        justify-content: space-around;
        align-items: center;
        width: 100%;
        flex-wrap: wrap;
        gap: 20px;
        margin-top: 100px;
    }
    .paw-btn {
        width: 40vw;  /* Кнопка почти половину экрана */
        max-width: 250px;
        aspect-ratio: 1/1; 
        border: none;
        cursor: pointer;
        border-radius: 50%; 
        overflow: hidden;
        background-color: transparent;
        background-size: cover;
        background-position: center;
        box-shadow: 0 10px 15px rgba(0,0,0,0.3);
        transition: transform 0.15s, box-shadow 0.15s, filter 0.15s;
        position: relative;
    }
    .paw-btn:hover {
        filter: brightness(1.2);
        transform: translateY(-5px);
        box-shadow: 0 15px 20px rgba(0,0,0,0.35);
    }
    .paw-btn:active {
        transform: translateY(3px) scale(0.95);
        box-shadow: 0 5px 10px rgba(0,0,0,0.2);
    }
    .paw-label {
        margin-top: 10px;
        font-size: 18px;
        font-weight: bold;
        color: #333;
    }
</style>
</head>
<body>

<!-- Новый заголовок -->
<h1 class="felix-style-title">Animal Fest Moldova 2026</h1>

<!-- Кнопки лапок -->
<div class="paw-container">
  <div>
    <button class="paw-btn" id="addPhoto" style="background-image: url('°•♡Pata de gato♡•°.jpeg');"></button>
    <div class="paw-label">Добавить фото</div>
  </div>
  <div>
    <button class="paw-btn" id="vote" style="background-image: url('°•♡Pata de gato♡•°.jpeg');"></button>
    <div class="paw-label">Голосовать</div>
  </div>
</div>

<script>
    const tg = window.Telegram.WebApp;
    tg.expand();

    function addPhoto() {
        tg.sendData(JSON.stringify({ action: "add_photo" }));
    }

    function vote() {
        tg.sendData(JSON.stringify({ action: "vote" }));
    }

    document.getElementById("addPhoto").onclick = addPhoto;
    document.getElementById("vote").onclick = vote;
</script>

</body>
</html>
