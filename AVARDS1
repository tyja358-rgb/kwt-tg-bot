<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<title>Pet Vote</title>
<script src="https://telegram.org/js/telegram-web-app.js"></script>
<style>
    body {
        font-family: Arial, sans-serif;
        text-align: center;
        margin: 0;
        padding: 50px 10px;
        background-color: #FFF8F0;
    }

    /* Заголовок PNG с анимацией */
    .festival-title {
        display: block;
        width: 90%;
        max-width: 800px;
        margin: 0 auto 50px auto;
        animation: glow 2s infinite alternate ease-in-out;
        filter: drop-shadow(0 0 10px rgba(255,200,0,0.8)) drop-shadow(0 0 5px rgba(255,140,0,0.6));
    }

    @keyframes glow {
        0% {
            filter: drop-shadow(0 0 5px rgba(255,200,0,0.6)) drop-shadow(0 0 3px rgba(255,140,0,0.4));
        }
        50% {
            filter: drop-shadow(0 0 15px rgba(255,200,0,0.9)) drop-shadow(0 0 10px rgba(255,140,0,0.7));
        }
        100% {
            filter: drop-shadow(0 0 8px rgba(255,200,0,0.7)) drop-shadow(0 0 5px rgba(255,140,0,0.5));
        }
    }

    /* Кнопки лапок */
    .paw-container {
        display: flex;
        justify-content: space-around;
        align-items: center;
        width: 100%;
        flex-wrap: wrap;
        gap: 20px;
        margin-top: 50px;
    }
    .paw-btn {
        width: 40vw;  
        max-width: 250px;
        aspect-ratio: 1/1; 
        border: none;
        cursor: pointer;
        border-radius: 50%; 
        overflow: hidden;
        background-color: transparent;
        background-size: cover;
        background-position: center;
        box-shadow: 0 10px 15px rgba(0,0,0,0.3);
        transition: transform 0.15s, box-shadow 0.15s, filter 0.15s;
        position: relative;
    }
    .paw-btn:hover {
        filter: brightness(1.2);
        transform: translateY(-5px);
        box-shadow: 0 15px 20px rgba(0,0,0,0.35);
    }
    .paw-btn:active {
        transform: translateY(3px) scale(0.95);
        box-shadow: 0 5px 10px rgba(0,0,0,0.2);
    }
    .paw-label {
        margin-top: 10px;
        font-size: 18px;
        font-weight: bold;
        color: #333;
    }
</style>
</head>
<body>

<!-- Новый заголовок через PNG -->
<img src="animal-fest-title.png" alt="Animal Fest Moldova 2026" class="festival-title">

<!-- Кнопки лапок -->
<div class="paw-container">
  <div>
    <button class="paw-btn" id="addPhoto" style="background-image: url('°•♡Pata de gato♡•°.jpeg');"></button>
    <div class="paw-label">Добавить фото</div>
  </div>
  <div>
    <button class="paw-btn" id="vote" style="background-image: url('°•♡Pata de gato♡•°.jpeg');"></button>
    <div class="paw-label">Голосовать</div>
  </div>
</div>

<script>
    const tg = window.Telegram.WebApp;
    tg.expand();

    function addPhoto() {
        tg.sendData(JSON.stringify({ action: "add_photo" }));
    }

    function vote() {
        tg.sendData(JSON.stringify({ action: "vote" }));
    }

    document.getElementById("addPhoto").onclick = addPhoto;
    document.getElementById("vote").onclick = vote;
</script>

</body>
</html>
